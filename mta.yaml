_schema-version: "3.2"
ID: memmanagnew
description: Memory Management application.
version: 0.0.1
modules:
- name: memmanagnew-destination-content
  type: com.sap.application.content
  requires:
  - name: memmanagnew-destination-service
    parameters:
      content-target: true
  - name: memmanagnew-html5-repo-host
    parameters:
      service-key:
        name: memmanagnew-html5-repo-host-key
  - name: uaa_memmanagnew
    parameters:
      service-key:
        name: uaa_memmanagnew-key
  parameters:
    content:
      instance:
        destinations:
        - Name: memmanagnew_html_repo_host
          ServiceInstanceName: memmanagnew-html5-app-host-service
          ServiceKeyName: memmanagnew-html5-repo-host-key
          sap.cloud.service: memmanagnew
        - Authentication: OAuth2UserTokenExchange
          Name: memmanagnew_uaa
          ServiceInstanceName: memmanagnew-xsuaa-service
          ServiceKeyName: uaa_memmanagnew-key
          sap.cloud.service: memmanagnew
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: memmanagnew-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: memmanagnew-html5-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - memmanagnew.zip
      name: memmanagnew
      target-path: resources/
- name: memmanagnew
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: memmanagnew-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: memmanagnew-destination-service
    service-plan: lite
- name: uaa_memmanagnew
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: memmanagnew-xsuaa-service
    service-plan: application
- name: memmanagnew-html5-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: memmanagnew-html5-app-host-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
